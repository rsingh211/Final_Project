<%= form_with(model: beat) do |form| %>
  <% if beat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beat.errors.count, "error") %> prohibited this beat from being saved:</h2>

      <ul>
        <% beat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :genre, style: "display: block" %>
    <%= form.text_field :genre %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.label :license_type, style: "display: block" %>
    <%= form.text_field :license_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
